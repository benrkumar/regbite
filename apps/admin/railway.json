{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "NODE_ENV=development pnpm install && pnpm build --filter=@regbite/admin && cp -r apps/admin/.next/static apps/admin/.next/standalone/apps/admin/.next/static && (cp -r apps/admin/public apps/admin/.next/standalone/apps/admin/public 2>/dev/null || true)"
  },
  "deploy": {
    "startCommand": "HOSTNAME=0.0.0.0 node apps/admin/.next/standalone/apps/admin/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
