{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "NODE_ENV=development pnpm install && pnpm build --filter=@regbite/app && cp -r apps/app/.next/static apps/app/.next/standalone/apps/app/.next/static && (cp -r apps/app/public apps/app/.next/standalone/apps/app/public 2>/dev/null || true)"
  },
  "deploy": {
    "startCommand": "HOSTNAME=0.0.0.0 node apps/app/.next/standalone/apps/app/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
