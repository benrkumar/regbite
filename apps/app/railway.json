{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "NODE_ENV=development pnpm install && pnpm build --filter=@regbite/app && cp -r apps/app/.next/static apps/app/.next/standalone/apps/app/.next/ && (cp -r apps/app/public apps/app/.next/standalone/apps/app/ 2>/dev/null || true)"
  },
  "deploy": {
    "startCommand": "(./node_modules/.bin/prisma db push --schema=packages/database/prisma/schema.prisma --accept-data-loss || true) && HOSTNAME=0.0.0.0 node apps/app/.next/standalone/apps/app/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
